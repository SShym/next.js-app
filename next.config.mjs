/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['eljzbzkg5d36iyau.public.blob.vercel-storage.com'],
    formats: ['image/avif', 'image/webp'],
  },
};

export default nextConfig;
